<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup rules for Android 12+ (API 31+). Older versions use backup_rules.xml.

  Only settings are backed up or transferred. The personal dictionaries under
  files/engines/ — learned words, learned bigrams and saved phrases — are the user's
  writing habits and stay on the device.

  The same rule is applied to both cloud backup and direct device-to-device transfer:
  the dictionaries should not leave the phone by either route.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="settings.xml" />
        <exclude domain="file" path="engines" />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="settings.xml" />
        <exclude domain="file" path="engines" />
    </device-transfer>
</data-extraction-rules>
