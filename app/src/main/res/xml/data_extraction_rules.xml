<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup rules for Android 12+ (API 31+). Older versions use backup_rules.xml.

  These are allowlists: once an <include> is present, ONLY what it lists is backed up
  or transferred. Settings are included; the personal dictionaries under
  files/engines/ — learned words, bigrams and saved phrases — are excluded by omission
  and never leave the phone, by cloud backup or by device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="settings.xml" />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="settings.xml" />
    </device-transfer>
</data-extraction-rules>
